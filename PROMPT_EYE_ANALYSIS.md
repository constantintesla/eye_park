# Промпт для создания системы анализа движения глаз, моргания и мимики вокруг глаз

## Описание проекта

Создай систему анализа видео для выявления симптомов неврологических расстройств (болезнь Паркинсона, инсульт, рассеянный склероз и др.) на основе анализа движения глаз, моргания и мимики вокруг глаз. Система должна иметь такую же структуру и архитектуру, как проект `audio_park`, но работать с видео вместо аудио.

## Структура проекта (аналогично audio_park)

```
eye_park/
├── bot.py                 # Telegram бот для загрузки видео
├── api.py                 # API сервер с веб-интерфейсом
├── parkinson_eye_analyzer.py  # Основной анализатор движений глаз
├── feature_extractor.py   # Извлечение визуальных признаков
├── symptom_analyzer.py    # Анализ симптомов на основе признаков
├── video_processor.py     # Обработка и предобработка видео
├── start_bot.py          # Запуск бота
├── start_api.py          # Запуск API
├── index.html            # Главная страница веб-интерфейса
├── results.html          # Страница со списком всех результатов
├── visualization.html    # Страница визуализации параметров
├── requirements.txt      # Зависимости
├── results.json          # Хранилище результатов (создается автоматически)
├── results/              # Директория с сырыми данными каждого анализа
│   └── {result_id}/      # Папка для каждого анализа
│       ├── original.mp4  # Исходное видео
│       ├── landmarks_data.json  # Данные о ключевых точках лица
│       ├── eye_tracking_data.json  # Данные о движении глаз
│       └── blink_analysis.json  # Данные о моргании
├── docker-compose.yml    # Docker конфигурация
├── Dockerfile            # Docker образ
├── README.md             # Документация
└── .env.example          # Пример переменных окружения
```

## Основные компоненты

### 1. video_processor.py
**Аналогично audio_processor.py**

Класс `VideoProcessor` для обработки видео:
- `load_video(file_path)` - загрузка видео с ресемплированием до стандартного разрешения (например, 1280x720)
- `detect_face(video)` - обнаружение лица в кадрах
- `extract_frames(video, fps_target=30)` - извлечение кадров с целевым FPS
- `normalize_lighting(frames)` - нормализация освещения
- `get_landmarks(video)` - получение ключевых точек лица (MediaPipe Face Mesh)
- `segment_eye_regions(landmarks)` - сегментация областей глаз

**Зависимости:**
- `opencv-python` (cv2) - обработка видео
- `mediapipe` - обнаружение лица и ключевых точек
- `numpy` - численные операции

### 2. feature_extractor.py
**Аналогично feature_extractor.py**

Класс `FeatureExtractor` для извлечения визуальных признаков:

#### Признаки движения глаз:
- `eye_movement_velocity` - скорость движения глаз (градусы/сек)
- `saccade_frequency` - частота саккад (саккад/сек)
- `saccade_amplitude` - амплитуда саккад (градусы)
- `smooth_pursuit_ratio` - соотношение плавного слежения к саккадам
- `fixation_duration` - средняя длительность фиксаций (мс)
- `fixation_stability` - стабильность фиксаций (стандартное отклонение)
- `gaze_direction_variation` - вариация направления взгляда
- `horizontal_eye_movement_range` - диапазон горизонтальных движений
- `vertical_eye_movement_range` - диапазон вертикальных движений

#### Признаки моргания:
- `blink_rate` - частота моргания (морганий/мин)
- `blink_duration` - средняя длительность моргания (мс)
- `blink_amplitude` - амплитуда закрытия глаза (процент)
- `inter_blink_interval` - средний интервал между морганиями (сек)
- `blink_incomplete_ratio` - соотношение неполных морганий
- `eye_closure_velocity` - скорость закрытия глаза (мс)
- `eye_opening_velocity` - скорость открытия глаза (мс)

#### Признаки мимики вокруг глаз:
- `eyebrow_movement_range` - диапазон движения бровей
- `eyelid_droop` - опущение века (ptosis) в пикселях
- `periorbital_muscle_activity` - активность окологлазных мышц
- `asymmetry_left_right` - асимметрия между левым и правым глазом
- `facial_expression_variation` - вариация выражения лица
- `eye_widening_range` - диапазон расширения глаз
- `squint_frequency` - частота прищуривания

#### Дополнительные признаки:
- `head_movement_compensation` - компенсация движений головы
- `pupil_size_variation` - вариация размера зрачка (если доступно)
- `gaze_convergence` - конвергенция взгляда (для бинокулярного анализа)

**Методы:**
- `extract_all_features(video, landmarks)` - извлечение всех признаков
- `_extract_eye_movement_features(landmarks, timestamps)` - признаки движения глаз
- `_extract_blink_features(landmarks, timestamps)` - признаки моргания
- `_extract_facial_features(landmarks, timestamps)` - признаки мимики
- `_detect_saccades(eye_positions, timestamps)` - обнаружение саккад
- `_detect_fixations(eye_positions, timestamps)` - обнаружение фиксаций
- `_detect_blinks(eye_landmarks, timestamps)` - обнаружение морганий

**Зависимости:**
- `opencv-python`
- `mediapipe`
- `numpy`
- `scipy` - для фильтрации сигналов
- `scikit-learn` - для кластеризации фиксаций

### 3. symptom_analyzer.py
**Аналогично symptom_analyzer.py**

Класс `SymptomAnalyzer` для анализа симптомов:

#### Пороговые значения (на основе исследований):
```python
THRESHOLDS = {
    # Движение глаз
    'saccade_frequency': 2.0,  # <2.0 саккад/сек - гипокинезия
    'saccade_amplitude': 5.0,   # <5.0 градусов - микросаккады
    'smooth_pursuit_ratio': 0.3,  # <0.3 - нарушение плавного слежения
    'fixation_stability': 2.0,  # >2.0 градусов - нестабильность фиксаций
    
    # Моргание
    'blink_rate': 10.0,  # <10 морганий/мин - редкое моргание (блефароспазм)
    'blink_rate_max': 30.0,  # >30 морганий/мин - частое моргание
    'blink_duration': 400.0,  # >400 мс - длительное моргание
    'blink_incomplete_ratio': 0.2,  # >20% - неполные моргания
    
    # Мимика
    'eyelid_droop': 2.0,  # >2.0 пикселей - птоз
    'asymmetry_left_right': 0.15,  # >15% - асимметрия
    'periorbital_muscle_activity': 0.5,  # <0.5 - гипомимия
}
```

#### Симптомы для оценки (0-3: Нет/Легкий/Умеренный/Тяжелый):
- `hypokinesia` - гипокинезия (снижение частоты саккад)
- `saccadic_abnormalities` - аномалии саккад (микросаккады, гипометрические саккады)
- `smooth_pursuit_deficit` - дефицит плавного слежения
- `fixation_instability` - нестабильность фиксаций
- `blink_abnormalities` - аномалии моргания (редкое/частое/неполное)
- `eyelid_droop` - птоз (опущение века)
- `facial_asymmetry` - асимметрия лица
- `hypomimia` - гипомимия (снижение мимики)

**Методы:**
- `analyze(features)` - основной анализ симптомов
- `_score_hypokinesia(features)` - оценка гипокинезии
- `_score_saccadic_abnormalities(features)` - оценка аномалий саккад
- `_score_blink_abnormalities(features)` - оценка аномалий моргания
- `_assess_pd_risk(exceeded_thresholds, symptom_scores, features)` - оценка риска ПД
- `_generate_report(features, symptom_scores, exceeded_thresholds)` - генерация отчета

### 4. parkinson_eye_analyzer.py
**Аналогично parkinson_analyzer.py**

Класс `ParkinsonEyeAnalyzer` - главный координатор:

**Методы:**
- `analyze_video_file(file_path, save_raw=True, result_id=None)` - полный анализ видео
- `_calculate_emsi(features)` - расчет EMSI (Eye Movement Severity Index)
  - Формула: `EMSI = 0.15 × Saccade_Freq + 0.20 × Blink_Rate - 0.10 × Fixation_Stability - 0.25 × Asymmetry + 10.0`
  - Интерпретация аналогична DSI:
    - **+2…+5**: Нормальные движения глаз
    - **0…+2**: Легкие нарушения
    - **-2…0**: Умеренные нарушения (PD 1-2)
    - **<-2**: Тяжелые нарушения (PD 3-5)
- `_generate_recommendation(risk_level, risk_probability, exceeded_thresholds, features)` - рекомендации
- `_clean_json_values(obj)` - очистка от NaN/inf для JSON

**Структура результата:**
```python
{
    "video_summary": {
        "duration_sec": float,
        "fps": int,
        "resolution": [width, height],
        "frames_count": int
    },
    "features": {
        # Все извлеченные признаки
    },
    "emsi": {
        "emsi_score": float,
        "emsi_range": str,
        "emsi_breakdown": {...},
        "interpretation": {...}
    },
    "symptom_scores": {
        # Оценки симптомов 0-3
    },
    "risk_probability": float,
    "risk_level": "Low/Medium/High",
    "recommendation": str,
    "report": [str],  # Текстовый отчет
    "raw_data": {
        "result_id": str,
        "data_directory": str,
        "files": {...}
    }
}
```

### 5. api.py
**Аналогично api.py**

Flask API сервер с эндпоинтами:
- `POST /api/analyze` - анализ видео файла
- `GET /api/results` - получение всех результатов
- `POST /api/results` - сохранение результата
- `GET /api/results/<index>` - получение конкретного результата
- `POST /api/recalculate-all` - пересчет всех результатов
- `GET /api/visualization/<index>` - данные для визуализации
- `GET /api/video/<index>` - получение видео файла
- `GET /api/stats` - статистика
- `GET /api/export/csv` - экспорт в CSV
- `GET /api/export/json` - экспорт в JSON
- `GET /api/export/html` - экспорт в HTML

**Визуализация должна включать:**
- График движения глаз (траектория взгляда)
- График частоты моргания
- График амплитуды саккад
- График стабильности фиксаций
- Видео с наложенными ключевыми точками (опционально)

### 6. bot.py
**Аналогично bot.py**

Telegram бот для загрузки видео:
- Команда `/start` - приветствие
- Команда `/analyze` - начало анализа
- Обработка видео файлов (MP4, AVI, MOV)
- Отправка отчета пользователю
- История отчетов через `/history`

**Текст инструкции для записи видео:**
```
📹 Пожалуйста, запишите видео, выполняя следующие действия:

1. Сядьте перед камерой на расстоянии 50-70 см
2. Убедитесь, что ваше лицо хорошо освещено
3. Смотрите прямо в камеру в течение 5 секунд
4. Медленно переводите взгляд влево, затем вправо, затем вверх, затем вниз
5. Следите глазами за воображаемым объектом, движущимся по кругу
6. Быстро переводите взгляд между двумя точками (влево-вправо) 5 раз
7. Моргните несколько раз естественным образом
8. Завершите запись

Длительность видео: 30-60 секунд
Формат: MP4, AVI, MOV
```

### 7. Веб-интерфейс

**index.html** - главная страница:
- Сводная таблица результатов
- Клик по строке показывает детальный отчет
- Кнопка "Пересчитать все результаты"
- Экспорт результатов (CSV, JSON, HTML)

**results.html** - список всех результатов:
- Карточки с результатами
- Кнопка визуализации для каждого результата

**visualization.html** - визуализация:
- График траектории движения глаз
- График частоты моргания
- График амплитуды саккад
- График стабильности фиксаций
- Видео с наложенными ключевыми точками (если доступно)
- Воспроизведение исходного видео

## Технические требования

### Зависимости (requirements.txt):
```
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
aiogram>=3.0.0
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
python-dotenv>=1.0.0
Pillow>=10.0.0
```

### Docker конфигурация:
- Аналогично `docker-compose.yml` из audio_park
- Два сервиса: `api` и `bot`
- Общие volumes для `results.json` и `results/`

### Переменные окружения (.env.example):
```
TELEGRAM_BOT_TOKEN=your_bot_token
API_URL=http://api:5000
API_PORT=5000
FLASK_ENV=development
DEBUG=True
```

## Исследования и пороговые значения

Система должна быть основана на исследованиях:
- **Leigh & Zee 2015**: "The Neurology of Eye Movements" - нормальные значения саккад и фиксаций
- **Antoniades et al. 2013**: "Eye movement abnormalities in Parkinson's disease" - аномалии при ПД
- **Bologna et al. 2013**: "Blink rate and blink timing in Parkinson's disease" - моргание при ПД
- **Fasano et al. 2012**: "Facial hypomimia in Parkinson's disease" - гипомимия при ПД

**Нормальные значения:**
- Частота саккад: 2-4 саккад/сек
- Амплитуда саккад: 5-20 градусов
- Частота моргания: 15-20 морганий/мин
- Длительность моргания: 100-400 мс
- Стабильность фиксаций: <1.0 градуса

**Патологические значения (ПД):**
- Частота саккад: <2.0 саккад/сек (гипокинезия)
- Амплитуда саккад: <5.0 градусов (микросаккады)
- Частота моргания: <10 или >30 морганий/мин
- Длительность моргания: >400 мс
- Стабильность фиксаций: >2.0 градуса

## Важные замечания

1. **Качество видео**: Требуется минимум 30 FPS, разрешение 720p или выше
2. **Освещение**: Лицо должно быть хорошо освещено для точного обнаружения ключевых точек
3. **Позиционирование**: Лицо должно занимать 30-50% кадра
4. **Длительность**: Рекомендуется 30-60 секунд для достаточного количества данных
5. **Система предназначена для исследовательских целей** и не заменяет медицинскую диагностику

## Дополнительные функции (опционально)

- Калибровка камеры для точного измерения углов движения глаз
- Бинокулярный анализ (отдельно для левого и правого глаза)
- Анализ размера зрачка (если разрешение позволяет)
- Обнаружение нистагма (непроизвольные движения глаз)
- Анализ конвергенции взгляда (для диагностики косоглазия)

## Структура данных для визуализации

```python
{
    "eye_trajectory": {
        "time": [float],  # секунды
        "x": [float],  # горизонтальная позиция (градусы или пиксели)
        "y": [float]   # вертикальная позиция
    },
    "blink_data": {
        "time": [float],
        "blink_events": [{"start": float, "end": float, "duration": float}],
        "blink_rate": float
    },
    "saccade_data": {
        "time": [float],
        "saccade_events": [{"start": float, "end": float, "amplitude": float, "velocity": float}],
        "saccade_frequency": float
    },
    "fixation_data": {
        "time": [float],
        "fixation_events": [{"start": float, "end": float, "duration": float, "position": [x, y]}],
        "fixation_stability": float
    },
    "video_url": "/api/video/<index>"
}
```

## Пример использования

```python
from parkinson_eye_analyzer import ParkinsonEyeAnalyzer

analyzer = ParkinsonEyeAnalyzer()
result = analyzer.analyze_video_file("video.mp4")
print(result)
```

## Критерии успеха

Система должна:
1. ✅ Извлекать все указанные визуальные признаки
2. ✅ Оценивать симптомы по шкале 0-3
3. ✅ Рассчитывать EMSI (Eye Movement Severity Index)
4. ✅ Определять риск ПД (Low/Medium/High)
5. ✅ Сохранять сырые данные (видео, ключевые точки, траектории)
6. ✅ Предоставлять веб-интерфейс для просмотра результатов
7. ✅ Работать через Telegram бота
8. ✅ Экспортировать результаты (CSV, JSON, HTML)
9. ✅ Визуализировать траектории движения глаз и моргания
10. ✅ Поддерживать пересчет всех результатов
